import styled from "styled-components";

export const SCIndex = styled.section`
  .error-wrapper {
    text-align: center;

    p {
      margin-top: ${({ theme }) => theme.spaces.m.mobileSize};
    }
  }
`;
